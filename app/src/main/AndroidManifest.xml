<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wisdom.partybuilding">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".base.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- android:theme="@style/Theme.AppCompat.NoActionBar"> -->


        <!-- android:theme="@style/AppTheme"> -->
        <activity android:name=".mvp.common.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".mvp.home.activity.BaseWebActivity" />
        <activity android:name=".mvp.home.activity.LoginActivity" />
        <activity android:name=".mvp.home.activity.SearchActivity" />
        <activity android:name=".mvp.mine.activity.AboutUsActivity" />
        <activity android:name=".mvp.mine.activity.ChangePasswordActivity" />
        <activity android:name=".mvp.common.WebViewCurrencyActivity" />
        <activity android:name=".mvp.mine.activity.MaterialActivity" />
        <activity android:name=".mvp.home.activity.TidingsActivity" />
        <activity android:name=".mvp.home.activity.PayPartyFeesActivity" />
        <activity android:name=".mvp.home.activity.ConfirmPaymentActivity" />
        <activity android:name=".mvp.home.activity.DetailActivity" />
        <activity android:name=".mvp.home.activity.DynamicActivity" />
        <activity android:name=".mvp.home.activity.PartyAffairsActivity" />
        <activity android:name=".mvp.upcoming.activity.ApplyActivity" />
        <activity android:name=".mvp.upcoming.activity.UrnPositiveActivity" />
        <activity android:name=".mvp.upcoming.activity.TransferOutActivity" />
        <activity android:name=".mvp.upcoming.activity.TransferActivity" />
        <activity android:name=".mvp.upcoming.activity.MakeReadyActivity" />
        <activity android:name=".mvp.upcoming.activity.GrowingActivity" />
        <activity android:name=".Main2Activity"></activity>
    </application>

</manifest>