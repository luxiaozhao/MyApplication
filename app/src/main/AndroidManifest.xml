<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wisdom.partybuilding">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".base.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- android:theme="@style/Theme.AppCompat.NoActionBar"> -->


        <!-- android:theme="@style/AppTheme"> -->
        <activity android:name=".mvp.activity.commonactivity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".mvp.activity.home.BaseWebActivity" />
        <activity android:name=".mvp.activity.login.LoginActivity" />
        <activity android:name=".mvp.activity.home.SearchActivity" />
        <activity android:name=".mvp.activity.mine.AboutUsActivity" />
        <activity android:name=".mvp.activity.login.ChangePasswordActivity" />
        <activity android:name=".mvp.activity.commonactivity.WebViewCurrencyActivity" />
        <activity android:name=".mvp.activity.mine.MaterialActivity" />
        <activity android:name=".mvp.activity.adapter.home.TidingsActivity" />
        <activity android:name=".mvp.activity.home.PayPartyFeesActivity" />
        <activity android:name=".mvp.activity.home.ConfirmPaymentActivity" />
        <activity android:name=".mvp.activity.home.DetailActivity"></activity>
    </application>

</manifest>